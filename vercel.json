{
  "buildCommand": "npm install -g pnpm && pnpm install && pnpm --filter parent-portal run build",
  "outputDirectory": "apps/parent-portal/.next",
  "env": {
    "VITE_SUPABASE_URL": "@supabase_url",
    "VITE_SUPABASE_ANON_KEY": "@supabase_anon_key"
  }
}
